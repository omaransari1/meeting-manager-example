<div ng-app="app">
  <div ng-controller="meetingsCtrl" ng-init="setup()">
    <h1><a href="/meetings">All meetings</a></h1>
    <a href="/meetings/new">Create new meeting</a>
    Search by meeting name: <input ng-model="nameFilter">
    <div ng-repeat="meeting in meetings | filter: {name: nameFilter}">
      <h3><a href="/meetings/{{ meeting.id }}">{{ meeting.name }}</a></h3>
      <p>
        Tags:
        <a ng-repeat="tag in meeting.tags" href="/meetings?tag={{ tag.name }}">{{ tag.name }}</a>  
      </p>
      <p>Address: {{ meeting.address }}</p>
      <p>Start time: {{ meeting.start_time }}</p>
      <p>End time: {{ meeting.end_time }}</p>
    </div>
  </div>
</div>
<!-- <a href="/meetings">All meetings</a>
<h2><a href="/meetings/new">Create new meeting</a></h2>
<% @meetings.each do |meeting| %>
  <h3><a href="/meetings/<%= meeting.id %>"><%= meeting.name %></a></h3>
  <p>
    Tags:
    <% meeting.tags.each do |tag| %>
      <a href="/meetings?tag=<%= tag.name %>"><%= tag.name %></a>  
    <% end %>
  </p>
  <p>Address: <%= meeting.address %></p>
  <p>Start time: <%= meeting.start_time %></p>
  <p>End time: <%= meeting.end_time %></p>
<% end %>
 -->